CREATE TABLE products (
    descrip TEXT NOT NULL,
    cod SERIAL,
    PRIMARY KEY (cod)
)

CREATE TABLE products2 (
    descrip TEXT NOT NULL,
    cod SERIAL,
    PRIMARY KEY (cod)
)

CREATE TABLE products3 (
    descrip TEXT NOT NULL,
    cod SERIAL,
    PRIMARY KEY (cod)
)